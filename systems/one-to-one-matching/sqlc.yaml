version: "2"

sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/postgres/query"
    gen:
      go:
        package: "db"
        out: "internal/postgres/db"
        sql_package: "pgx/v5"
        emit_interface: true